{
  "name": "qnext-corp",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 5000",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 5000",
    "start": "node server.mjs",
    "lint:links": "node scripts/check-link-text.mjs",
    "prebuild": "node scripts/check-link-text.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "astro": "^4.16.19",
    "compression": "^1.8.1",
    "express": "^5.2.1"
  }
}
